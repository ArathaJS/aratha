/* global J$ */

var x = J$.readInput(), y = J$.readInput(), z = J$.readInput();
var r = "";

if (x + y === "a" && r.concat(x, y, z) === "abc") {
    console.log(x);
    console.log(y);
    console.log(z);
}
